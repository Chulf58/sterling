---
description: Capture a raw note into the knowledge store (H11 extracts candidates automatically).
---

Call `knowledge_create` with type `note`: `raw_text` is the user's words VERBATIM (no paraphrase), `captured_at` now, `capture_source` "command", `derived` []. Report the id and any extraction result H11 surfaced.

Routing — a note is a verbatim relay of a user statement and nothing else. A task the user wants done goes to `/sterling:todo` (the board), never here. Knowledge YOU concluded is born structured — `decision` / `anti_pattern` / `research_finding` / an article reconcile — never parked in a note.
