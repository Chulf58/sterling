---
description: Add a todo to the Sterling board.
---

Call `board_add` with `source: "user"` and the user's text verbatim (plus `file_keys`/`priority` only if the user stated them). Confirm with the record id.
